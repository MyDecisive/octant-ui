name: Publish Helm Chart to GHCR

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  helm-publish:
    uses: MyDecisive/ci/.github/workflows/helm-publish.yml@main
    secrets: inherit # pass all secrets