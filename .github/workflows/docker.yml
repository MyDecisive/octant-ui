name: Docker push for main branch

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  docker-push:
    uses: DecisiveAI/ci/.github/workflows/docker-ghcr.yaml@rlaw/docker-image-to-ghcr
    secrets: inherit # pass all secrets