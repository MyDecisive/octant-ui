name: Docker push for main branch

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  docker-push:
    uses: MyDecisive/ci/.github/workflows/docker-ghcr.yaml@tv/slack_version_publish
    secrets: inherit # pass all secrets
